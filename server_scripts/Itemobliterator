ServerEvents.recipes(event => {
  event.remove({ output: 'modid:bad_item' })
})

JEIEvents.hideItems(event => {
  event.hide('modid:bad_item')
})

PlayerEvents.inventoryChanged(event => {
  const item = event.item
  if (item.id === 'modid:bad_item') {
    item.count = 0
  }
})
